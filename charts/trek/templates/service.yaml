apiVersion: v1
kind: Service
metadata:
  name: {{ include "odyssey.fullname" . }}
  labels:
    app: {{ include "odyssey.name" . }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 3000
      protocol: TCP
      name: http
  selector:
    app: {{ include "odyssey.name" . }}
